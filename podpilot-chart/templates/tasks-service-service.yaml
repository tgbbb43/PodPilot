apiVersion: v1
kind: Service
metadata:
  name: tasks-service
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: tasks-service
  ports:
    - port: {{ .Values.tasksService.port }}
      targetPort: {{ .Values.tasksService.port }}
