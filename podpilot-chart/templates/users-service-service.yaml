apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: users-service
  ports:
    - port: {{ .Values.usersService.port }}
      targetPort: {{ .Values.usersService.port }}
