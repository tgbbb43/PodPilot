apiVersion: v1
kind: Service
metadata:
  name: notifications-service
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: notifications-service
  ports:
    - port: {{ .Values.notificationsService.port }}
      targetPort: {{ .Values.notificationsService.port }}
