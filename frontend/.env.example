# Used only by `npm run dev` (Vite's dev-server proxy in vite.config.js).
# The built app itself never reads these - it only calls relative /api/* paths.
USERS_SERVICE_URL=http://localhost:4001
TASKS_SERVICE_URL=http://localhost:4002
NOTIFICATIONS_SERVICE_URL=http://localhost:4003
